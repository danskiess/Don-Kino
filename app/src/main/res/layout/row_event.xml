<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/event_container"
    android:layout_width="match_parent"
    android:layout_height="@dimen/grid_height"
    android:padding="@dimen/grid_padding">

    <ImageView
        android:id="@+id/event_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Adjust the gradient opacity + scale imageview-->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="@dimen/grid_details_container_height"
        android:padding="@dimen/grid_details_padding"
        android:layout_alignParentBottom="true"
        android:background="@drawable/gradient_drawable">

        <TextView
            android:id="@+id/event_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="@dimen/grid_detail_font_size"
            android:textColor="@color/primary_white_text"
            tools:text="Title"/>
    </LinearLayout>
</RelativeLayout>